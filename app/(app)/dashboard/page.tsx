import { Panel } from "@/components/ui/Panel";

// TODO(dashboard slice): replace with the real Overview screen — primary directive,
// metrics bar, active builds, my day / inbound, standby / deployed, review queue,
// event log. See PLAN.md "Dashboard" and the Shipyard mockup's `isDash` section.
export default function DashboardPage() {
  return (
    <Panel className="p-8 text-ink-3">
      Overview — under construction.
    </Panel>
  );
}
