import { Panel } from "@/components/ui/Panel";

// TODO(tasks slice): replace with the real Queue screen — today/upcoming/overdue/
// all/completed views, project vs standalone filter. See PLAN.md "Standalone
// Personal Tasks" / "Overdue Tasks" and the Shipyard mockup's `isTasks` section.
export default function TasksPage() {
  return (
    <Panel className="p-8 text-ink-3">
      Queue — under construction.
    </Panel>
  );
}
