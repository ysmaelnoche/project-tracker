import { Panel } from "@/components/ui/Panel";

// TODO(github slice): replace with the real Source Control screen — cross-project
// pull requests / branches / commit feed, repository list, link-a-repo form. See
// PLAN.md "GitHub Integration" and the Shipyard mockup's `isSource` section.
export default function SourcePage() {
  return (
    <Panel className="p-8 text-ink-3">
      Source control — under construction.
    </Panel>
  );
}
