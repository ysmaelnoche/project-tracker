import { Panel } from "@/components/ui/Panel";

// TODO(projects slice): replace with the real "New project" form (name, type,
// priority, target date) — enters at status "pending". See PLAN.md "Creating a
// Project" and the Shipyard mockup's project create form (`fm.isProject`).
export default function NewProjectPage() {
  return (
    <Panel className="p-8 text-ink-3">
      New project — under construction.
    </Panel>
  );
}
