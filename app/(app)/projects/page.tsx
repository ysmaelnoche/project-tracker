import { Panel } from "@/components/ui/Panel";

// TODO(projects slice): replace with the real Fleet screen — class/stage filters,
// project list with progress gauges. See PLAN.md "Projects View" and the Shipyard
// mockup's `isProjects` section.
export default function ProjectsPage() {
  return (
    <Panel className="p-8 text-ink-3">
      Fleet — under construction.
    </Panel>
  );
}
