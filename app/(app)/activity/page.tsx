import { Panel } from "@/components/ui/Panel";

// TODO(polish slice): replace with the real Event Log screen — full chronological
// activity feed (app + GitHub events). See PLAN.md "Recent Activity" and the
// Shipyard mockup's `isActivity` section.
export default function ActivityPage() {
  return (
    <Panel className="p-8 text-ink-3">
      Event log — under construction.
    </Panel>
  );
}
