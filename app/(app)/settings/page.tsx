import { Panel } from "@/components/ui/Panel";

// TODO(github slice): replace with the real Config screen — automation rule
// toggles (commit linking, branch binding, first-commit-activates, PR-merge-closes,
// tag-marks-production [default off], stale-branch-alert) and sign-out. See
// PLAN.md "GitHub Data Strategy" and the Shipyard mockup's `isSettings` section.
export default function SettingsPage() {
  return (
    <Panel className="p-8 text-ink-3">
      Config — under construction.
    </Panel>
  );
}
