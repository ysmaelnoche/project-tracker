# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# testing
/coverage
/test-results
/playwright-report
/blob-report
/playwright/.cache

# env files (never commit real credentials)
.env
.env*.local

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# typescript
*.tsbuildinfo
next-env.d.ts

# supabase local
/supabase/.branches
/supabase/.temp

# editors
.vscode/*
!.vscode/extensions.json
.idea

# nested agent worktrees (created by the Agent tool's isolation:"worktree" —
# each is its own git worktree/repo checkout; never lint/typecheck/commit into it)
.claude/
